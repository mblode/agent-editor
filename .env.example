# =============================================================================
# Agent Editor — Environment Variables
# Copy to .env and fill in values before running
# =============================================================================

# --- Anthropic ---
# Your Anthropic API key (required for agent execution)
ANTHROPIC_API_KEY=sk-ant-...

# --- Encryption ---
# AES-256-GCM key for encrypting stored API keys (32 bytes, base64 encoded)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=

# --- Database ---
# SQLite (local dev): file:./dev.db
# Turso (production): libsql://your-db.turso.io
DATABASE_URL=file:./dev.db
# DATABASE_AUTH_TOKEN=  # Required for Turso

# --- Sprites.dev (optional, for sandbox execution) ---
# Get your token at sprites.dev
# SPRITES_TOKEN=your-org/id/token/...

# --- API Server ---
PORT=3001
CORS_ORIGIN=http://localhost:3000
NODE_ENV=development

# --- Web App ---
NEXT_PUBLIC_API_URL=http://localhost:3001
